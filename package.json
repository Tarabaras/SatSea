{
  "name": "satsea-opnet",
  "version": "0.1.0",
  "private": true,
  "description": "SatSea — NFT Marketplace on Bitcoin L1 (OP_NET)",
  "scripts": {
    "build:contract": "cd contracts && npm run build",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd contracts && npm install && cd ../frontend && npm install",
    "deploy": "cd scripts && npx ts-node deploy.ts"
  }
}
